package com.hillel.language.basis15.practice;

import com.hillel.language.basis15.MyScanner;

import java.io.File;

public class RecursiveScanner implements MyScanner{

    public RecursiveScanner(File directory) {
    }

    @Override
    public int scan(String word) {

        // traverse directory hierarchy
//        FileScanner fileScanner = new FileScanner();
//        int result = fileScanner.scan(word);

        return 0;
    }
}
